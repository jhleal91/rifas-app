# Configuración de Base de Datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rifas_digital
DB_USER=postgres
DB_PASSWORD=tu_password

# Base de datos de test (se usa automáticamente cuando NODE_ENV=test)
TEST_DB_NAME=rifas_digital_test

# Configuración del Servidor
PORT=5001
NODE_ENV=development

# JWT Secret (generar uno seguro en producción)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Frontend URL (para CORS y redirects)
FRONTEND_URL=http://localhost:3000

# Stripe Configuration
# Obtener estas claves en: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email Configuration (opcional)
# EMAIL_SERVICE=resend
# EMAIL_API_KEY=re_...
# EMAIL_FROM=noreply@sorteohub.com

# Sentry (opcional, para producción)
# SENTRY_DSN=https://...
